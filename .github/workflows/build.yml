name: Build project

on:
  push:

jobs:
  Build-Project:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2.11.1
      - name: Run gradle build
        run: ./gradlew build
